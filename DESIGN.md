# DESIGN — 정토회(jungto.org) 스타일 가이드

> 참고 사이트: <https://www.jungto.org/> (정토회 공식 홈페이지)
> 본 문서는 jungto.org의 CSS/구조 분석을 바탕으로 우리 기도 앱에 적용할
> 디자인 시스템을 정리한 것입니다. (2026-08 기준)

---

## 1. 전체 무드 (Mood)

- **밝고 깨끗한 화이트 베이스** — 대부분의 섹션이 흰 배경, 콘텐츠가 숨 쉬는 여백
- **온화한 사찰 감성** — 난폭한 채도 대신 부드러운 파스텔 톤의 액센트
- **에디토리얼한 구성** — 섹션을 1px 보더(`#ebe5e6`)로 구분하고 위아래 여백을 크게 줌
- **절제된 인터랙션** — 화려한 애니메이션 대신 호버 시 색 변화/인셋 섀도우 같은 미묘한 피드백
- 다크 섹션을 부분적으로 사용해(`#3f3e3c`) 수행/기도 같은 특별 섹션에 깊이감 부여

## 2. 컬러 팔레트

| 역할 | 색상 | HEX | 사용처 |
| --- | --- | --- | --- |
| 브랜드 주액센트 | 오렌지 | `#f26522` | 활성 네비게이션, 메인 포인트 |
| 보조 액센트 | 청록(틸) | `#519ea6` | 기도/수행 섹션, 링크, 호버 인셋 테두리 |
| 버튼 액센트 | 주황(진함) | `#ee8545` | 검색/CTA 버튼 (흰 글자) |
| 메인 텍스트 | 검정 | `#000` / `#333` | 본문, 카드 텍스트 |
| 보조 텍스트 | 그레이 | `#666` / `#999` | 설명, 날짜, 부가 정보 |
| 제목 텍스트 | 다크 | `#262626` | 섹션 제목 |
| 배경(기본) | 화이트 | `#ffffff` | 헤더, 카드, 전체 배경 |
| 카드 배경 | 연그레이 | `#f6f6f6` / `#f8f8f8` / `#fbfbfb` | 콘텐츠 박스 |
| 보더 | 연베이지 | `#ebe5e6` / `#e1e1e1` | 구분선, 테두리 |
| 다크 섹션 | 차콜 | `#3f3e3c` | 기도(행자) 특별 섹션 배경 |
| 수행용 브라운 | 브라운 | `#442a20` / `#a96d55` | 기도 캘린더, 촛불 이미지 톤 |

### 색상 사용 규칙

- 배경은 기본 화이트, 섹션 구분은 1px 보더로 (그림자/카드 그림자 지양)
- 포인트 컬러는 **한 화면에 하나** — 네비게이션이면 오렌지, 기도 섹션이면 청록
- 다크 섹션(`#3f3e3c`)은 전체 화면의 일부 영역에만 사용해 대비와 깊이 연출
- 링크는 기본 블랙 → 호버 시 `#519ea6` + 밑줄

## 3. 타이포그래피

| 항목 | 값 |
| --- | --- |
| 기본 폰트 | **나눔스퀘어 (NSquare)** — L(300) / R(500) / B(700) / 900 |
| 기본 자간 | `letter-spacing: -0.03em` (한글 가독성 최적화) |
| 네비게이션 | 22px, weight 450, 자간 `-0.05em` |
| 섹션 제목(h3) | 28px, line-height 1.4, 하단 마진 30px |
| 본문 | 18px, line-height 1.5 |
| 대형 헤로 텍스트 | 28~34px, weight 300 (가볍게) |
| 소형 라벨/날짜 | 12~16px, 그레이 `#999` |

> 웹 폰트 적용 시: 나눔스퀘어를 CDN으로 로드하거나, 구글 폰트의
> **Noto Sans KR** 또는 **Gowun Batang**(명조 느낌)으로 대체 가능

## 4. 레이아웃

- **메인 컨테이너: `max-width: 1080px`, 중앙 정렬** (검색 오버레이는 1200px)
- 헤더 구조: 로고 영역(높이 90px, 하단 보더) + GNB(높이 60px)
- 네비 아이템: 폭 136px, 중앙 정렬, 활성 항목은 `#f26522` + **3px 언더라인 바**
- 섹션 패딩: 상하 60~140px의 넉넉한 여백, 섹션 간 1px 보더 분리
- 카드 그리드: 4열(`calc(25% - 10px)`), 호버 시 `inset 0 0 0 4px #519ea6` 인셋 테두리
- 콘텐츠 박스: `#f8f8f8` 배경 + `3px 상단 보더`(청록)로 강조
- 타임라인/히스토리: 중앙 1px 세로선(`#d6d6d6`)

## 5. 컴포넌트

### 버튼

- **플랫(flat), 라운드 없음** — 둥근 모서리 대신 각진 사각형
- CTA: 배경 `#ee8545`, 흰 글자, 예: 150×60px
- 텍스트 링크 버튼: 밑줄(보더) 방식, 호버 시 포인트 컬러
- 호버: 배경 색 한 단계 진하게 (`#ee8545` → 조금 더 진한 주황)

### 헤더/네비게이션

- 흰 배경, 1px 하단 보더
- 대메뉴 22px, 활성 항목 언더라인 3px (`#f26522`)
- 서브메뉴: 드롭다운 흰 배경, `border-top: 1px solid #ebe5e6`

### 카드

- 배경 `#f6f6f6`, 패딩 48px 35px
- 호버: `inset` 4px 테두리 (`#519ea6`) — **외곽 테두리 없이 안쪽 라인만**
- 제목 30px `#666`, 설명 18px

### 다크 섹션 (기도/수행)

- 배경 `#3f3e3c`, 흰/밝은 텍스트
- 브라운 톤(`#442a20`, `#a96d55`) 이미지와 조화
- 흰 텍스트 버튼: 흰 글자 + 흰 밑줄 보더

## 6. 인터랙션 패턴

| 패턴 | 구현 |
| --- | --- |
| 네비 활성 표시 | 언더라인 3px 바 (`#f26522`) |
| 카드 호버 | `box-shadow: inset 0 0 0 4px #519ea6` |
| 링크 호버 | `#519ea6` 색상 + `text-decoration: underline` |
| 버튼 호버 | 배경색 한 단계 진하게 |
| 페이지네이션 | 활성 페이지만 검정 볼드, 나머지 그레이 |

## 7. 우리 앱(절하기) 적용 현황 ✅

**2026-08-01 적용 완료** — 정토회 스타일이 실제 앱에 반영되어 있습니다.

적용된 항목:

- `src/index.css` — 정토회 팔레트로 교체:
  - `--bg: #ffffff`, `--text: #333333`, `--text-h: #262626`
  - `--accent: #f26522` + `--accent-hover: #d9551d` (호버 시 한 단계 진하게)
  - `--secondary: #519ea6`, `--border: #ebe5e6`
  - 폰트: 나눔스퀘어 우선 + `letter-spacing: -0.03em`
  - 배경 그라데이션 제거 → 단색 화이트 (다크 모드 블록 제거, 정토회는 라이트 온리)
- `src/App.css` — 버튼/타이포 정토회식으로:
  - 절하기 버튼: `border-radius: 0` 플랫 사각형, 배경 `#f26522` + 흰 글자, 호버 `#d9551d`
  - 제목: weight 300, `letter-spacing: -0.03em`
  - 부제: `#666` 보조 그레이
  - 하단 `1px solid #ebe5e6` 섹션 구분 보더
- `index.html` — 나눔스퀘어 폰트 CDN 로드
  `https://cdn.jsdelivr.net/gh/moonspam/NanumSquare@master/nanumsquare.css`
- **플레이어 화면 추가 (2화면 구조)** — 시작 화면("기도하기" 버튼) → 미디어 플레이어 전환:
  - 시작 화면: 제목 + 부제 + 플랫 주황 "기도하기" 버튼
  - 플레이어: 재생/일시정지 플랫 버튼(96×96, `#f26522`), 진행 바(주황 슬라이더),
    시간 표시(mm:ss), "처음으로" 텍스트 링크 버튼(호버 시 `#519ea6`)
  - 오디오 요소는 두 화면이 공유 (전환 시 리마운트 방지)
- **트랙 선택 구조 (2트랙)** — 시작 화면에서 `[절하기] [천일결사]` 나란히 선택:
  - `절하기` → `/bow.opus`(우선)/`/bow.m4a`(폴백), `천일결사` → `/cheonil.opus`(우선)/`/cheonil.m4a`(폴백)
  - 선택 시 오디오 src를 즉시 교체 후 재생, 플레이어 제목에 트랙명 표시
- **오디오 포맷 (Opus 우선 + AAC 폴백)**:
  - `canPlayType('audio/ogg; codecs="opus"')`으로 Opus 지원 감지 → 미지원(데스크톱 사파리)은 AAC 자동 폴백
  - 코덱: Opus/AAC 모두 모노 48kbps, Cloudflare Pages 25MiB 제한 준수

미적용(보류) 항목:

- `#3f3e3c` 다크 섹션/푸터 — 단일 화면이라 생략, 추후 화면이 늘어나면 적용

## 8. 참고 출처

- 정토회 홈페이지 HTML/CSS 분석 (2026-08-01 추출)
  - `https://www.jungto.org/data/app_web/css/base/base.css`
  - `https://www.jungto.org/data/app_web/css/base/fonts.css`
  - `https://www.jungto.org/data/app_web/css/style.min.css`
- 폰트: 나눔스퀘어(NanumSquare) woff2 자체 호스팅
